class NewsCategory {
  String key; //APIye gönderilecek olan kategori değeridir(ing)
  String title; //UI tarafında üstte görüntülenecek kategori değeri(tur)
  NewsCategory({required this.key, required this.title});
}
